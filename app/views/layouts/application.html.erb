<!DOCTYPE html>
<html>
 <%= render 'shared/head' %>
  <body>
    <%= render 'shared/header' %>
  	
  	<div class="container">
  		<% flash.each do |key, value| %>
  		<%= content_tag :div, value, class: "alert alert-#{key}" %>
  		<% end %>

    	<%= yield %>
    </div>
  </body>
</html>